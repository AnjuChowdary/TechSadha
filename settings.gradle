pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url("https://ajduties.jfrog.io/artifactory/aj-network-service/")
            credentials {
                username = "anonymous"
                password = ""
            }
        }
    }
}
rootProject.name = "TechSadha"
include ':app'
